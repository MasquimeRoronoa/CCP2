{% extends 'baseAdmin.html.twig' %}

{% block title %}Modifier une Categories{% endblock %}

{% block body %}
<div class="content-wrapper">
    <h1>Modifier une Categories</h1>

    {{ include('categories/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('categories_index') }}"><button class="btn btn-primary"> Retour à la Liste </button></a>

    {{ include('categories/_delete_form.html.twig') }}
</div>
{% endblock %}

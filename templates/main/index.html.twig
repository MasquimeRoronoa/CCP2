{% extends 'base.html.twig' %}

{% block title %}Rudy Art Wall{% endblock %}

{% block body %}
    <main class="row overflow-auto" id="corps">
        <div class="col-12 h-100 d-flex justify-content-center align-items-center">

            <!-- Photo aleatoire provenant de la bdd-->
             {% for photos in photo %}

            <img src="{{ vich_uploader_asset(photos, "imageFile") }}" style="width: 1920px; height: 1080px;" alt="">

            {% endfor %}


        </div>

    </main>
{% endblock %}

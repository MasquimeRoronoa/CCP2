{% extends 'baseAdmin.html.twig' %}

{% block title %}Photographe{% endblock %}

{% block body %}
<div class="content-wrapper">
    <h1>Photographe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ photographe.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ photographe.NomPhotographe }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ photographe.PrenomPhotographe }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ photographe.MailPhotographe }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ photographe.DescPhotographe }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td><img src="{{ vich_uploader_asset(photographe, "imageFile") }}" style="width: 200px; height: 150px;" alt=""></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('photographe_index') }}"><button class="btn btn-primary">Retour à la Liste</button> </a>

    <a href="{{ path('photographe_edit', {'id': photographe.id}) }}"><button class="btn btn-danger">Editer</button> </a>

    {{ include('photographe/_delete_form.html.twig') }}
</div>
{% endblock %}

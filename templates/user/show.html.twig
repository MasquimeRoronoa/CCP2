{% extends 'baseAdmin.html.twig' %}

{% block title %}Utilisateurs{% endblock %}

{% block body %}
    <div class="content-wrapper">
    <h1>Utilisateurs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Mot de Passe</th>
                <td>{{ user.password }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_index') }}"><button class="btn btn-primary">Retour à la Liste</a>

    <a href="{{ path('user_edit', {'id': user.id}) }}"><button class="btn btn-danger">Editer</button> </a>

    {{ include('user/_delete_form.html.twig') }}
    </div>
{% endblock %}
